# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (jamais commites - cf .env.example)
.env
.env*.local

# dumps SQL locaux (JAMAIS commiter : contiennent les donnees prod des sites)
backup*.sql
backup*.dump
*.pgdump
dump-*.sql
pg_dump-*.sql

# scripts jetables (injection ponctuelle, migration de donnees ad-hoc) ET leurs
# sorties (dumps/sauvegardes JSON de donnees prod). Ils lisent
# SUPABASE_SERVICE_ROLE_KEY, contiennent souvent des UUID de sites en dur, et
# leurs dumps contiennent des donnees prod ; JAMAIS a commiter. Cf. audit S1
# (2026-09-10) + bascule niveaux Le Bignon (2026-09-14).
_tmp_*.js
_tmp_*.ts
_tmp_*.mjs
_tmp_*.json
scratch_*.js
scratch_*.ts
scratch_*.mjs
scratch_*.json
inject_*.js

# prisma
/prisma/*.db

# typescript
*.tsbuildinfo

# os / editor
Thumbs.db
.idea/
.vscode/

# claude code (reglages personnels ; launch.json est partage)
.claude/settings.local.json
