# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (jamais commites - cf .env.example)
.env
.env*.local

# dumps SQL locaux (JAMAIS commiter : contiennent les donnees prod des sites)
backup*.sql
backup*.dump
*.pgdump
dump-*.sql
pg_dump-*.sql

# scripts jetables (injection ponctuelle, migration de donnees ad-hoc). Ils
# lisent SUPABASE_SERVICE_ROLE_KEY et contiennent souvent des UUID de sites
# en dur ; JAMAIS a commiter. Cf. audit S1 (2026-09-10).
_tmp_*.js
_tmp_*.ts
scratch_*.js
scratch_*.ts
inject_*.js

# prisma
/prisma/*.db

# typescript
*.tsbuildinfo

# os / editor
Thumbs.db
.idea/
.vscode/

# claude code (reglages personnels ; launch.json est partage)
.claude/settings.local.json
